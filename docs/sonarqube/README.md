# Evidencias SonarQube

Guarda aqui las capturas solicitadas por la rubrica:

| Momento | Archivo |
| --- | --- |
| Antes de refactorizar | `antes.png` |
| Despues de refactorizar | `despues.png` |

Despues de subir las capturas, completa la tabla de metricas del `README.md` principal con los valores reales del dashboard de SonarQube.

## Estado actual

Los archivos `antes.png` y `despues.png` existen como plantillas temporales porque en esta maquina no esta disponible Docker/SonarQube (`docker` no se reconoce y `localhost:9000` no responde). Para cumplir la rubrica al 100%, reemplaza esas plantillas por capturas reales del dashboard de SonarQube despues de ejecutar el analisis inicial y el final.
